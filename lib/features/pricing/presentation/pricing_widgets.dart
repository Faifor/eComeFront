import 'package:flutter/material.dart';

class PricingPlaceholderTile extends StatelessWidget {
  const PricingPlaceholderTile({super.key});

  @override
  Widget build(BuildContext context) {
    return const ListTile(title: Text('Pricing item'));
  }
}
