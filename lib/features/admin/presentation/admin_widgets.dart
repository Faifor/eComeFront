import 'package:flutter/material.dart';

class AdminPlaceholderTile extends StatelessWidget {
  const AdminPlaceholderTile({super.key});

  @override
  Widget build(BuildContext context) {
    return const ListTile(title: Text('Admin item'));
  }
}
