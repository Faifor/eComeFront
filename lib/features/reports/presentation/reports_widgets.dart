import 'package:flutter/material.dart';

class ReportsPlaceholderTile extends StatelessWidget {
  const ReportsPlaceholderTile({super.key});

  @override
  Widget build(BuildContext context) {
    return const ListTile(title: Text('Reports item'));
  }
}
