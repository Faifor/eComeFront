import 'package:flutter/material.dart';

class OrdersPlaceholderTile extends StatelessWidget {
  const OrdersPlaceholderTile({super.key});

  @override
  Widget build(BuildContext context) {
    return const ListTile(title: Text('Orders item'));
  }
}
