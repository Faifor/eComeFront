import 'package:flutter/material.dart';

class CatalogPlaceholderTile extends StatelessWidget {
  const CatalogPlaceholderTile({super.key});

  @override
  Widget build(BuildContext context) {
    return const ListTile(title: Text('Catalog item'));
  }
}
