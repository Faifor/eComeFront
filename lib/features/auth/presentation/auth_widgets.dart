import 'package:flutter/material.dart';

class AuthPlaceholderTile extends StatelessWidget {
  const AuthPlaceholderTile({super.key});

  @override
  Widget build(BuildContext context) {
    return const ListTile(title: Text('Auth item'));
  }
}
